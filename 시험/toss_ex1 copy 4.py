from collections import defaultdict
def solution(invitationPairs):
    dic = defaultdict(int)
    for pair in invitationParis:
        invitor = pair[0]
        dic[invitor] = dic[invitor] + 1
    answer = []
    return answer